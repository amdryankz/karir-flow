import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Check Offering",
};

export default function CheckOfferingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
