import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Upload Offer Letter",
};

export default function UploadOfferingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
